{
  "name": "student-migration",
  "version": "0.1.0",
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  },
  "private": true,
  "scripts": {
    "import": "bun run src/index.ts --tui",
    "migrate": "bun run src/index.ts",
    "migrate:interactive": "bun run src/index.ts --tui",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "dependencies": {
    "@opentui/core": "^0.4.0",
    "exceljs": "^4.4.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
